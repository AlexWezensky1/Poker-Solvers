__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Claude Code scratch worktrees (a second checkout of this repo)
.claude/worktrees/

# scripts/showdown_chart.py writes these as it walks. Commit the chart once the
# run says complete; the checkpoint it writes through never is.
web/static/showdown-chart.json
web/static/showdown-chart.json.part
