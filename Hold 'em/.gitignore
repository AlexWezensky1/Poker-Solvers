__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Claude Code scratch worktrees (a second checkout of this repo)
.claude/worktrees/

# scripts/preflop_chart.py writes the chart as it walks. The finished chart is
# committed and shipped; the half-written one it checkpoints through is not.
web/static/preflop-chart.json.part
