__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Claude Code scratch worktrees (a second checkout of this repo)
.claude/worktrees/

# Written by scripts/preflop_chart.py as it walks. Delete these two lines
# and commit the file once the run says complete.
web/static/preflop-chart.json
web/static/preflop-chart.json.part
