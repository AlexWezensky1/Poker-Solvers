__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Claude Code scratch worktrees (a second checkout of this repo)
.claude/worktrees/

# scripts/showdown_chart.py checkpoints through this while it walks. The chart
# it lands on is committed and shipped; the half-written one is not.
web/static/showdown-chart.json.part
